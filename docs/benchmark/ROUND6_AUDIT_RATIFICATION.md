# 第六轮审计裁定:Round 6 预注册予以批准(2026-07-13)

**核验方式**:预注册文档逐条审阅 + 门禁证据抽查(内存试点日志逐字核对、
禁用脚本实测 exit 2、固件集 manifest/随机化排程/可复现性检查在档)

## 裁定

1. **`ROUND6_MECHANISM_PREREGISTRATION.md` 批准执行。**
   三限分离(运行时 depth / 编译期 capacity / 资源上限)、10 超块种子随机化、
   run 为独立单元、Holm 校正、右删失处理、主张边界——达到顶会方法学标准。
2. **公开记录:执行会话纠正了审计方的设计混淆。** 审计方早期 E8 设计让扫
   `HISTORY_SIZE_STATEFUL`(编译期容量),不会改变运行时驱逐行为;预注册
   固定 capacity=48、扫运行时 depth 才是正确操作化。对抗式协作机制有效。
3. 门禁证据全部属实:d40×h250 最坏组合真机 22 项通过、运行时余量 131,872 B、
   +13,376 B 静态开销;12 变体内容寻址归档;危险旧脚本已物理禁用。

## 两条技术备注(入分析时须注明,不阻塞执行)

- **跨轮可比性**:本轮 depth=5 格的 capacity=48/max_samples=48 与 Round-4
  矩阵的 capacity=10/max_samples=30 存在配置差;跨轮引用 depth-5 数据时
  必须带此限定(manifest 已记录,分析文稿别漏)。
- **无修复锚点**:H1-H3 无需 BE 对照格;Round-4 的 BE@15% b2h 数据
  (送达≈85%,RTT p95 31.7ms)即"零修复机制"锚点,分析中直接引用。

## 正式数据前实施更正

后续执行器审计确认,Linux `tc gact random netrand` 使用整数逆概率分母;
名义 15% 目标实际配置为 `1/7=14.285714%`,并非精确 15% netem。
该更正在任何 Round 6 正式数据产生前写入预注册。条件名、逐次台账、PCAP
和 `tc -s` 状态均同时记录名义值与有效值。冻结固件、因子水平、随机排程、
假设和统计分析均未改变。

## 通往投稿的剩余路径(按序)

| # | 项 | 状态/估时 |
|---|---|---|
| 1 | 冒烟门禁 12 格×3 | 完成,36/36 PASS |
| 2 | 正式矩阵 12 格×N30(随机化超块) | 完成,360/360 接受 |
| 3 | 机制归因分析:depth×HB 交互图(p95 / 未解决请求数) | 完成,H2 支持,H1/H3 不支持 |
| 4 | P4 独立环境窗口重复 0/5/15% | 半天机时 |
| 5 | E3 章节决策:7 月已有前/后修证据与死锁 pcap 图,按论文框架决定收录或缓办 | 写作期 |
| 6 | 上游 PR(GUID 熵修复)+ 手动 Wireshark 证据图(作者分工) | 写作期 |

## 项目总评(第七次审查)

方法学已达顶会就绪;瓶颈从"严谨性"转移到"机时 + 写作"。
自 07-06 以来的完整轨迹:构建修复 → 可靠性修复(8/8)→ 三系统初测 →
采集事故与三轮返工 → 治理自升级 → 方向性传输发现(RELIABLE 被 BE 压制)→
预注册机制实验。**这条轨迹本身就是论文 artifact 叙事的一部分,保留全部台账。**
